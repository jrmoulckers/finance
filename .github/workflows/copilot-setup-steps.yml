# =============================================================================
# Copilot Setup Steps — agent environment bootstrap
# =============================================================================
#
# Validates the coding-agent environment and keeps the repo-specific setup steps discoverable in GitHub Actions.
# =============================================================================
name: 'Copilot Setup Steps'

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Validate workspace
        run: |
          echo "✅ Node.js $(node --version)"
          echo "✅ npm $(npm --version)"
          echo "✅ Workspace ready for Copilot coding agent"

      # Future setup steps (uncomment as platforms are added):
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.12"
      #
      # - name: Setup .NET
      #   uses: actions/setup-dotnet@v4
      #   with:
      #     dotnet-version: "9.0.x"
      #
      # - name: Setup Java/Kotlin
      #   uses: actions/setup-java@v4
      #   with:
      #     distribution: "temurin"
      #     java-version: "21"
